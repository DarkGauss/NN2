#include "util.h"

Eigen::MatrixXd& read_stdin();



Eigen::MatrixXd& read_stdin()
{
    int r, c;
    int numread;

    //figure out the dimenision of the array to read
    numread = scanf("%d", &r)
    Eigen::MatrixXd b;
    return b;
}